#include "context.h"
#include "wayland.h"

void wayland_window_zero(struct ctx * ctx) {
    (void)ctx;
}

void wayland_window_init(struct ctx * ctx) {
    (void)ctx;
}

void wayland_window_cleanup(struct ctx * ctx) {
    (void)ctx;
}
